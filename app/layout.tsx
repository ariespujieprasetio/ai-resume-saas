import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Veritik",
  description: "AI Resume Screening Platform that ranks and compares candidates instantly.",
  icons: {
    icon: "/favicon.jpeg",
  },
  openGraph: {
    title: "Veritik AI Hiring Assistant",
    description: "Rank and compare candidates instantly using AI.",
    images: ["/logo/logo-veritik.jpeg"],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}